package com.example.store.service;

import com.example.store.dto.PCDto;

import java.util.Collection;

public interface PCService {
    PCDto addDevice(PCDto dto);

    PCDto updateDevice(PCDto dto);

}

